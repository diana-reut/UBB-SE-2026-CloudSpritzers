<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CloudSpritzers1.Src.View.Faq.FAQAddEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CloudSpritzers1.Src.View.Faq"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource CardBackgroundBrush}" Padding="24">
        <Border MaxWidth="700"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{ThemeResource CardBackgroundBrush}"
                BorderBrush="{ThemeResource PrimaryBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="0">

            <StackPanel>
                <Border Background="{ThemeResource PrimaryLightBrush}"
                        Height="8"
                        CornerRadius="12,12,0,0"/>

                <StackPanel Padding="24" Spacing="18">

                    <TextBlock x:Name="PageTitleText"
                                Text="Add FAQ"
                               FontSize="28"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource PrimaryTextBrush}"/>

                    <TextBlock x:Name="PageSubtitleText"
                               Text="Create a frequently asked question entry"
                               FontSize="13"
                               Foreground="{ThemeResource SecondaryTextBrush}"
                               Margin="0,-8,0,4"/>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Category"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource PrimaryBrush}"/>
                        <Border Background="{ThemeResource InputBackgroundBrush}"
                                BorderBrush="{ThemeResource PrimaryMutedBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10,2">
                            <ComboBox x:Name="CategoryComboBox"
                                      PlaceholderText="Select category"
                                      MinWidth="300"
                                      MinHeight="44"
                                      Background="Transparent"
                                      RequestedTheme="Light"
                                      Foreground="{ThemeResource PrimaryTextBrush}"
                                      BorderThickness="0">
                                <ComboBoxItem Content="CheckIn"/>
                                <ComboBoxItem Content="Parking"/>
                                <ComboBoxItem Content="Baggage"/>
                                <ComboBoxItem Content="Tickets"/>
                                <ComboBoxItem Content="Facilities"/>
                               
                            </ComboBox>
                        </Border>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Question"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource PrimaryBrush}"/>
                        <Border Background="{ThemeResource InputBackgroundBrush}"
                                BorderBrush="{ThemeResource PrimaryMutedBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10,6">
                            <TextBox x:Name="QuestionTextBox"
                                     PlaceholderText="Enter the question"
                                     TextWrapping="Wrap"
                                     MinWidth="300"
                                     MinHeight="44"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{ThemeResource PrimaryTextBrush}">
                                <TextBox.Resources>
                                    <SolidColorBrush x:Key="TextControlBackground" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBackgroundDisabled" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBorderBrushFocused" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBorderBrushPointerOver" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBorderBrushDisabled" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlForeground" Color="{ThemeResource PrimaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlForegroundFocused" Color="{ThemeResource PrimaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="{ThemeResource PrimaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlForegroundDisabled" Color="{ThemeResource SecondaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlPlaceholderForeground" Color="{ThemeResource SecondaryTextColor}"/>
                                </TextBox.Resources>
                            </TextBox>
                        </Border>
                    </StackPanel>

                    <StackPanel Spacing="8">
                        <TextBlock Text="Answer"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{ThemeResource PrimaryBrush}"/>
                        <Border Background="{ThemeResource InputBackgroundBrush}"
                                BorderBrush="{ThemeResource PrimaryMutedBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10,6">
                            <TextBox x:Name="AnswerTextBox"
                                     PlaceholderText="Enter the answer"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     MinWidth="300"
                                     Height="140"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{ThemeResource PrimaryTextBrush}">
                                <TextBox.Resources>
                                    <SolidColorBrush x:Key="TextControlBackground" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBackgroundDisabled" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBorderBrushFocused" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBorderBrushPointerOver" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlBorderBrushDisabled" Color="Transparent"/>
                                    <SolidColorBrush x:Key="TextControlForeground" Color="{ThemeResource PrimaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlForegroundFocused" Color="{ThemeResource PrimaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlForegroundPointerOver" Color="{ThemeResource PrimaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlForegroundDisabled" Color="{ThemeResource SecondaryTextColor}"/>
                                    <SolidColorBrush x:Key="TextControlPlaceholderForeground" Color="{ThemeResource SecondaryTextColor}"/>
                                </TextBox.Resources>
                            </TextBox>
                        </Border>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Spacing="10"
                                Margin="0,8,0,0">

                        <Button x:Name="CancelButton"
                                Content="Cancel"
                                Click="CancelButton_Click"
                                Background="{ThemeResource InputBackgroundBrush}"
                                Foreground="{ThemeResource PrimaryBrush}"
                                BorderBrush="{ThemeResource PrimaryMutedBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="18,10">
                            <Button.Resources>
                                <SolidColorBrush x:Key="ButtonBackground" Color="{ThemeResource InputBackgroundColor}"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource PrimaryMutedColor}"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource PrimaryMutedColor}"/>
                                <SolidColorBrush x:Key="ButtonBackgroundDisabled" Color="{ThemeResource PageBackgroundColor}"/>
                                <SolidColorBrush x:Key="ButtonForeground" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonForegroundPressed" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonForegroundDisabled" Color="{ThemeResource SecondaryTextColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrush" Color="{ThemeResource PrimaryMutedColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="{ThemeResource PrimaryMutedColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="{ThemeResource PrimaryMutedColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushDisabled" Color="{ThemeResource BorderColor}"/>
                            </Button.Resources>
                        </Button>

                        <Button x:Name="SaveButton"
                                Content="Save Changes"
                                Click="SaveButton_Click"
                                Background="{ThemeResource PrimaryLightBrush}"
                                Foreground="White"
                                BorderThickness="0"
                                CornerRadius="8"
                                Padding="18,10">
                            <Button.Resources>
                                <SolidColorBrush x:Key="ButtonBackground" Color="{ThemeResource PrimaryLightColor}"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonBackgroundDisabled" Color="{ThemeResource PrimaryMutedColor}"/>
                                <SolidColorBrush x:Key="ButtonForeground" Color="White"/>
                                <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White"/>
                                <SolidColorBrush x:Key="ButtonForegroundPressed" Color="White"/>
                                <SolidColorBrush x:Key="ButtonForegroundDisabled" Color="{ThemeResource BorderColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrush" Color="{ThemeResource PrimaryLightColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushPressed" Color="{ThemeResource PrimaryColor}"/>
                                <SolidColorBrush x:Key="ButtonBorderBrushDisabled" Color="{ThemeResource PrimaryMutedColor}"/>
                            </Button.Resources>
                        </Button>

                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>